# Authorizer

#### A DB-Oriented password Manager that provides the potential to fetch a password in a third-party application by a URL call with the user's token and the password name.
i.e. http://servername.com/token={user_token}/name={password_name}

Also there is a management system with an Administrator Account that manages Users,Passwords and their between relationships (Access).